on reload:

    set question
        controller TICK
        presenter TICK

    set answers
        controller TICK
        presenter TICK

    set question revealed
        controller
        presenter

    set wrong answers
        controller
        presenter

    set revealed answers
        controller
        presenter

    set total points
        controller
        presenter

    set round over
        controller
        presenter
